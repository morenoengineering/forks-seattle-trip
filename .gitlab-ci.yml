# Publishes the contents of public/ as a static site via GitLab Pages.
# No build step needed since index.html is already a complete, self-contained file.
pages:
  stage: deploy
  script:
    - echo "Publishing static site"
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
